Here are the solutions to the provided coding exercises, including the necessary classes and methods:

### 1. GOAIR Security Coding Exercise

#### EntryUtility.java
```java
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class EntryUtility {
    public boolean validateEmployeeId(String employeeId) throws InvalidEntryException {
        if (employeeId.matches("GOAIR/[0-9]{4}")) {
            return true;
        } else {
            throw new InvalidEntryException("Invalid entry details");
        }
    }

    public boolean validateDuration(int duration) throws InvalidEntryException {
        if (duration >= 1 && duration <= 5) {
            return true;
        } else {
            throw new InvalidEntryException("Invalid entry details");
        }
    }
}
```

#### InvalidEntryException.java
```java
public class InvalidEntryException extends Exception {
    public InvalidEntryException(String message) {
        super(message);
    }
}
```

#### UserInterface.java
```java
import java.util.Scanner;

public class UserInterface {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        EntryUtility entryUtility = new EntryUtility();

        System.out.println("Enter the number of entries");
        int num = sc.nextInt();
        sc.nextLine(); // consume newline

        for (int i = 1; i <= num; i++) {
            System.out.printf("Enter entry %d details\n", i);
            String entryDetails = sc.nextLine();
            String[] details = entryDetails.split(":");

            if (details.length < 3) {
                System.out.println("Invalid entry details");
                continue;
            }

            String employeeId = details[0];
            String entryType = details[1];
            int duration;

            try {
                duration = Integer.parseInt(details[2]);
                entryUtility.validateEmployeeId(employeeId);
                entryUtility.validateDuration(duration);
                System.out.println("Valid entry details");
            } catch (NumberFormatException e) {
                System.out.println("Invalid entry details");
            } catch (InvalidEntryException e) {
                System.out.println("Invalid entry details");
            }
        }
        sc.close();
    }
}
```

### 2. Shop Validator Coding Exercise

#### GadgetValidatorUtil.java
```java
public class GadgetValidatorUtil {
    public boolean validateGadgetID(String gadgetID) throws InvalidGadgetException {
        if (gadgetID.matches("[A-Z][0-9]{3}")) {
            return true;
        } else {
            throw new InvalidGadgetException("Invalid gadget ID");
        }
    }

    public boolean validateWarrantyPeriod(int period) throws InvalidGadgetException {
        if (period >= 6 && period <= 36) {
            return true;
        } else {
            throw new InvalidGadgetException("Invalid warranty period");
        }
    }
}
```

#### InvalidGadgetException.java
```java
public class InvalidGadgetException extends Exception {
    public InvalidGadgetException(String message) {
        super(message);
    }
}
```

#### UserInterface.java
```java
import java.util.Scanner;

public class UserInterface {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        GadgetValidatorUtil gadgetValidator = new GadgetValidatorUtil();

        System.out.println("Enter the number of gadget entries");
        int num = sc.nextInt();
        sc.nextLine(); // consume newline

        for (int i = 1; i <= num; i++) {
            System.out.printf("Enter gadget %d details\n", i);
            String gadgetDetails = sc.nextLine();
            String[] details = gadgetDetails.split(":");

            if (details.length < 3) {
                System.out.println("Invalid gadget details");
                continue;
            }

            String gadgetID = details[0];
            String gadgetType = details[1];
            int warrantyPeriod;

            try {
                warrantyPeriod = Integer.parseInt(details[2]);
                gadgetValidator.validateGadgetID(gadgetID);
                gadgetValidator.validateWarrantyPeriod(warrantyPeriod);
                System.out.println("Warranty accepted, stock updated");
            } catch (NumberFormatException e) {
                System.out.println("Invalid gadget details");
            } catch (InvalidGadgetException e) {
                System.out.println(e.getMessage());
            }
        }
        sc.close();
    }
}
```

These implementations adhere to the specifications provided in the problem statements. Adjust as needed for your testing environment!
